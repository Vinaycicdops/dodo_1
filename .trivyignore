
# # Accepted non-exploitable findings (msgpack vendored inside pip & setuptools base image metadata)
# msgpack	Detected inside pip's vendored libraries rather than as an application dependency.
GHSA-6v7p-g79w-8964

# setuptools	CVE-2025-47273	Scanner detects metadata from the base image even though the application runtime uses setuptools 83.0.0.
CVE-2025-47273